import { TransactionsStatusDirective } from './transactions-status.directive';

describe('TransactionsStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new TransactionsStatusDirective();
    expect(directive).toBeTruthy();
  });
});
